import React from 'react';
import { H1, Section } from '../common/Typography';

const Hero = () => {
  return (
    <Section id="home">
      <H1>Splash</H1>
    </Section>
  );
};

export default Hero;
